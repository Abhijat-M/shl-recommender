fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
httpx==0.28.1
beautifulsoup4==4.12.3
lxml==5.3.0
sentence-transformers==3.3.1
faiss-cpu==1.9.0.post1
rank-bm25==0.2.2
# Gemini (default LLM) is reached over its REST API via httpx — no SDK
# dep needed.
# Groq is kept as an alternative backend (off by default). The SDK is
# optional; comment the line below if you don't plan to enable Groq.
groq==0.15.0
tenacity==9.0.0
orjson==3.10.13
numpy==1.26.4
python-dotenv==1.0.1
tiktoken==0.8.0

# dev / test
pytest==8.3.4
pytest-asyncio==0.25.0
pytest-cov==6.0.0
respx==0.22.0
